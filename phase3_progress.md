# Phase 3 — Unified designer shell

Completed:

- Added one Capture Profile Designer reachable from **More → Capture profiles (prototype)…**.
- Added Overview, Batch, per-document-type, and Test Capture navigation.
- Reused rule-set, field collection, and script/shared-source editors at both scopes.
- Added document-type add, copy, remove, and reorder commands.
- Kept Recognition Rules distinct from Document Start Rules.
- Added fallback and duplicate-rule validation before save.
- Added a non-destructive legacy migration preview and an end-to-end Test Capture action.
- Added per-file batch grouping, automatic export, and trigger-page-disposition controls.

The prototype is deliberately alongside the three legacy designers during comparison.
